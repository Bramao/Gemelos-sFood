<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.edu.unab.gemelosapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activity.AdminAgregarActivity"></activity>
        <activity android:name=".view.activity.AdminPedidosActivity" />
        <activity android:name=".view.activity.AdminEditarActivity" />
        <activity android:name=".view.activity.AdminListadoActivity" />
        <activity android:name=".view.activity.AdminMenuActivity" />
        <activity android:name=".view.activity.CarritoActivity" />
        <activity android:name=".view.activity.DescripcionActivity" />
        <activity android:name=".view.activity.ListadoActivity" />
        <activity android:name=".view.activity.MenuActivity" />
        <activity android:name=".view.activity.RegistroActivity" />
        <activity android:name=".view.activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>